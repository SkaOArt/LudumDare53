mc: There is nothing special in the trash can.
if {location1.paket_picket} == "true":
	[end_timeline]
if {location1.search_count} >= 3:
	mc: „Hey, there is something behind it!"
	[signal arg="searched_enough"]
